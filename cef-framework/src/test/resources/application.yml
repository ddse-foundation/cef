# Base Test Configuration

spring:
  application:
    name: cef-framework-test

# Logging for tests
logging:
  level:
    org.ddse.ml.cef: DEBUG
    org.springframework.jdbc.core: DEBUG
    org.springframework.jdbc: DEBUG
